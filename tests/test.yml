---
- hosts: localhost
  tasks:
    - shell: ip link add eth1 type dummy && ip link set up dev eth1 && ip a a 172.24.69.1 dev eth1
      tags:
        - skip_ansible_lint
- hosts: localhost
  vars_files:
    - generic_config.yml
    - keepalived_config.yml
  roles:
    - "{{ rolename | basename }}"

- name: Functional test
  hosts: localhost
  tasks:
    - shell: ping -c 4 172.24.69.1
      tags:
        - skip_ansible_lint
